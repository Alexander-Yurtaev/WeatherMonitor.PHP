### JetBrains IDEs (PhpStorm, IntelliJ, WebStorm) ###
# Covers user-specific stuff that shouldn't go into VCS
.idea/
# Оставьте закомментированной строку ниже, если хотите хранить общие настройки проекта (структуру папок, разметку)
# !.idea/.gitkeep

# Файлы пользовательских настроек (оставляем на случай, если не используем папку .idea целиком)
*.iws
*~
*.swp
*.swo

### PHP & Composer ###
/vendor/
composer.phar
.env
.env.local
.env.*.local

# PHPUnit
.phpunit.result.cache
.phpunit.cache/
coverage.xml

### Frontend (если используете Vue/React внутри PHP-проекта) ###
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dist/
build/

### OS generated files ###
.DS_Store
Thumbs.db

### Logs ###
*.log